# Local overrides for server/.env
# This file is loaded after .env and should NOT be committed.
# Useful for local-only settings or machine-specific secrets.

# Example: override a model or port for local testing
# OPENROUTER_API_KEY=sk-local-override
