{
  "buildCommand": "npm run build --workspace=app",
  "outputDirectory": "app/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
